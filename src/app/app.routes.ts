import { Routes } from '@angular/router';
import { ContestPage } from './pages/contest/contest.page';

export const routes: Routes = [
    {
        path:'',
        component: ContestPage
    }, 
];
 